---
# Snaps that ship with Ubuntu and are removed on every profile.
cleanup_snaps_absent:
  - { name: firefox, profiles: [personal, work] }

# Profile-filtered view of the catalog above.
cleanup_snaps_absent_selected: "{{ cleanup_snaps_absent | selectattr('profiles', 'contains', profile) | list }}"
